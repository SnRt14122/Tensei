// 此文件已废弃：词义检测的答题记录不再走"逐题调用 server action"的方式，
// 原因是每答一题就同步 await 一次网络请求（内部还做了两次串行的 Supabase 往返），
// 是"检测很慢"的主要原因。
//
// 现在的流程是：
// 1. 判分在前端完成，答题记录写入本地 IndexedDB 缓存（见 src/lib/localStore.ts）
// 2. 用户点击导航栏"同步"按钮后，才调用 src/app/sync/actions.ts 里的
//    syncQuizAttempts()，一次性批量同步所有本地记录到云端
//
// 这个文件保留为空文件（无法在当前环境中删除），不再被任何组件引用。
